#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int n = prices.size();
        if(n <= 1)return 0;
        int minp[n], maxp[n], dp[n];
        int ans = 0;
        int num;
        
        for(int i = 1;i < n;i++){

        }
        return ans;
    }
};