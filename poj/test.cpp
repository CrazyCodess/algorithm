#include <iostream>
#include <cstdio>
#include <cstring>
using namespace std;
int main(){
	int  c;
	while(cin>>c){
		cout<<c<<endl;
	}
	
	/*	for(i=1;i<n;i++){
		if(tel[i-1]==tel[i])cnt++;
		else if(tel[i-1]!=tel[i]||i==n) {
			if(cnt>1){
				printf("%d-%d %d\n", tel[i-1]/10000,tel[i-1]-(tel[i-1]/10000)*10000,cnt);
				f=1;
			}
			cnt=1;
		}
	}*/
			//sort(tel,tel+n);
	//cnt=1;
		//tel[i]=tem;
	return 0;
}